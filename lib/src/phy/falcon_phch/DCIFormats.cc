#include "falcon/phy/falcon_phch/DCIFormats.h"

//DCIFormat::DCIFormat(srslte_cell_t* cell,
//                     srslte_dl_sf_cfg_t* sf,
//                     srslte_dci_cfg_t* cfg,
//                     srslte_dci_format_t format, uint32_t globalIndex) :
//  format(format),
//  globalIndex(globalIndex)
//{
//  size = srslte_dci_format_sizeof(format, cell->nof_prb, cell->nof_ports);
//  // future version
//  //size = srslte_dci_format_sizeof(cell, sf, cfg, format);
//}

//DCIFormat::~DCIFormat() {}
